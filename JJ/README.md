
  # Design UI Components

  This is a code bundle for Design UI Components. The original project is available at https://www.figma.com/design/Qqzx4rzymyX64ex2Xz1nmq/Design-UI-Components.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  